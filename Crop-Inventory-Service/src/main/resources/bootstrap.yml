spring:
  application:
    name: crop-inventory-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: false
      retry:
        initial-interval: 3000
        max-interval: 10000
        multiplier: 1.2
        max-attempts: 6
